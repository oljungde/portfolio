import { HeaderOffsetDirective } from './header-offset.directive';

describe('HeaderOffsetDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderOffsetDirective();
    expect(directive).toBeTruthy();
  });
});
